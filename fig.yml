db:
    image: dockerfile/mongodb
    ports:
        - 27017
web:
    build: .
    command: npm start
    volumes:
        - .:/webapp_root
    ports:
        - 3000:3000
    links:
        - db
    environment:
        PORT: 3000
        MONGO_DATABASE: dev_db
