db:
    image: dockerfile/mongodb
    ports:
        - 27017
web:
    build: .
    command: sh start_dev_app.sh
    volumes:
        - .:/webapp_root
    ports:
        - 3000:3000
    links:
        - db
