var cMod = angular.module( 'myApp.controllers', [] );


cMod.controller( 'Charts', [ '$scope', function( $scope ) {

} ] );


cMod.controller( 'Transit', [ '$scope', function( $scope ) {

} ] );


cMod.controller( 'Weather', [ '$scope', function( $scope ) {

} ] );
